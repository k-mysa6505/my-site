---
import Layout from '../../layouts/Layout.astro';
import PostLayout from '../../layouts/PostLayout.astro';

const { slug } = Astro.params;  // URLから
---
<Layout>
    <PostLayout slug={slug} type='works' />
</Layout> 